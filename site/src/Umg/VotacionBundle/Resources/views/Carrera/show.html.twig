{% extends '::base.html.twig' %}

{% block titulo %}
    <h1 class="page-title">Carrera</h1>
{% endblock %}
{% block body -%}
    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Carrera</th>
                <td>{{ entity.Carrera }}</td>
            </tr>
        </tbody>
    </table>

      
    
        <a href="{{ path('carrera') }}" class="btn btn-primary">
            Volver a la lista
        </a>
    
    
        <a href="{{ path('carrera_edit', { 'id': entity.id }) }}" class="btn btn-primary">
            Editar
        </a>
    
    {{ form(delete_form) }}

{% endblock %}
