{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CarreraCurso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Pensumanio_id</th>
            </tr>
        </thead>
        <tbody>
        {% for curso in cursos %}
            <tr>
                <td>{{ curso.Codigo }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('carreracurso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
