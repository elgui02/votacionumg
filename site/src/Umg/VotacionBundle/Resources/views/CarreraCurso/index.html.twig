{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CarreraCurso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Campuscarrera_id</th>
                <th>Pensumanio_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('carreracurso_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.CampusCarreraid }}</td>
                <td>{{ entity.PensumAnioid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('carreracurso_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('carreracurso_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('carreracurso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
