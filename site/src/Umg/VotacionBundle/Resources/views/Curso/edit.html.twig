{% extends '::base.html.twig' %}

{% block titulo %}
    <h1 class="page-title">Editar Curso</h1>
{% endblock %}

{% block body -%}
    {{ form(edit_form) }}
        <a href="{{ path('curso') }}" class="btn btn-primary">
            Volver a la lista
        </a>
{{ form(delete_form) }}
{% endblock %}
