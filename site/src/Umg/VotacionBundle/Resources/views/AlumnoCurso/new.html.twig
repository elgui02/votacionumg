{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AlumnoCurso creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('alumnocurso') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
