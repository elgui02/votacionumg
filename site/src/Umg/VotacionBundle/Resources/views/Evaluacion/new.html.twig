{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evaluacion creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('evaluacion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
