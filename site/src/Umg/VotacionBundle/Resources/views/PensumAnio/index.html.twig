{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PensumAnio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Curso_id</th>
                <th>Pensum_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pensumanio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Cursoid }}</td>
                <td>{{ entity.Pensumid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pensumanio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('pensumanio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pensumanio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
