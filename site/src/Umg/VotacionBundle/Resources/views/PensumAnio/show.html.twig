{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PensumAnio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Curso_id</th>
                <td>{{ entity.Cursoid }}</td>
            </tr>
            <tr>
                <th>Pensum_id</th>
                <td>{{ entity.Pensumid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pensumanio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pensumanio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
