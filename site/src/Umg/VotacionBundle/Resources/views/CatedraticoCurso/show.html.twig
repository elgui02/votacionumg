{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CatedraticoCurso</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Catedratico_id</th>
                <td>{{ entity.Catedraticoid }}</td>
            </tr>
            <tr>
                <th>Campuscurso_id</th>
                <td>{{ entity.CampusCursoid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('catedraticocurso') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('catedraticocurso_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
