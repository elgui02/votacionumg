{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CatedraticoCurso list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Catedratico_id</th>
                <th>Campuscurso_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('catedraticocurso_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Catedraticoid }}</td>
                <td>{{ entity.CampusCursoid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('catedraticocurso_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('catedraticocurso_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('catedraticocurso_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
