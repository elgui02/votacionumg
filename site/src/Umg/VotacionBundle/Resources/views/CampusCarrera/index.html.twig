{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CampusCarrera list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Carrera_id</th>
                <th>Campus_id</th>
                <th>Jornada_id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('campuscarrera_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Carreraid }}</td>
                <td>{{ entity.Campusid }}</td>
                <td>{{ entity.Jornadaid }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('campuscarrera_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('campuscarrera_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('campuscarrera_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
