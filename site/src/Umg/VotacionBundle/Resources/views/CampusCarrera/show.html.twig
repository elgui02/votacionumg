{% extends '::base.html.twig' %}

{% block body -%}
    <h1>CampusCarrera</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Carrera_id</th>
                <td>{{ entity.Carreraid }}</td>
            </tr>
            <tr>
                <th>Campus_id</th>
                <td>{{ entity.Campusid }}</td>
            </tr>
            <tr>
                <th>Jornada_id</th>
                <td>{{ entity.Jornadaid }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('campuscarrera') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('campuscarrera_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
