{% extends '::base.html.twig' %}
{% block titulo %}
    Lista de campus
{% endblock %}
{% block body -%}

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Campus</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('campus_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.Campus }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('campus_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('campus_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('campus_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
