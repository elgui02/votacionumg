{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Campus creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('campus') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
